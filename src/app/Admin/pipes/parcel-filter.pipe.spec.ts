import { ParcelFilterPipe } from './parcel-filter.pipe';

describe('ParcelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ParcelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
